version: '3.9'

services:
  caddy:
    build: caddy
    restart: always
    ports:
      - 80:80
      - 443:443
    networks:
      - web
    environment:
      - CF_API_TOKEN=${CF_API_TOKEN}
    volumes:
      - ./caddy/conf/:/etc/caddy/
      - ./caddy/data:/data
      - ./mirror/prod/:/mnt/mirror
      - caddy_config:/config
  
  mirror-update:
    build: mirror
    restart: always
    volumes:
      - ./mirror/prod/:/mnt/mirror

volumes:
  caddy_config:
  netdataconfig:
  netdatalib:
  netdatacache:

networks:
  web: